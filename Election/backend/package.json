{
  "name": "election-assistant-backend",
  "version": "1.0.0",
  "description": "Backend API for Election Assistant",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gcloud run deploy election-assistant-backend --source . --platform managed --allow-unauthenticated --region=us-central1 --quiet"
  },
  "keywords": ["election", "voting", "assistant"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
